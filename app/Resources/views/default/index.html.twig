{% extends 'base.html.twig' %}

{% set sumSpecimens = 0 %}
{% block body %}
    <ul>
    {% for file in files %}
        <li>
            <a href="{{ path('viewfile', {'filename':file.getFilename, 'institutionCode':institutionCode }) }}">{{file.getFilename }}</a>&nbsp;
            {{'creele'|trans}} {{ file.getDiffs.getCTime|date("d/m/Y") }}&nbsp;
            {{'modifiele'|trans}} {{ file.getChoices.getMTime|date("d/m/Y") }}&nbsp;
        </li>
    {% endfor %}
    </ul>
{% endblock %}