{% extends '@App/base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="bs-callout bs-callout-danger">
            <h4>{{ "error.occured" | trans({}, 'exceptions') }}</h4>
            {{ 'searchDiffError' | trans({}, 'exceptions') }}
        </div>
    </div>


{% endblock %}
